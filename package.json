{
  "name": "aidrecabrera",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "biome wrangler dev",
    "deploy": "pnpm stats && biome wrangler deploy",
    "format": "echo 'Prettier is no longer used'",
    "stats": "biome tsm --env-file .env scripts/stats.ts",
    "lint": "echo 'Linting is handled differently now'",
    "types": "biome check"
  },
  "devDependencies": {
    "@biomejs/biome": "1.8.3",
    "@cloudflare/workers-types": "^4.20231218.0",
    "@types/node": "^20.11.2",
    "prettier": "^3.2.2",
    "tsm": "^2.3.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.22.4"
  },
  "dependencies": {
    "dotenv": "^16.4.0"
  }
}
